
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>دعوة زفاف ياسمين وأحمد</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Amiri:wght@400;700&family=Aref+Ruqaa:wght@400;700&family=Cairo:wght@400;600;700&display=swap" rel="stylesheet">
<style>
  * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
    -webkit-tap-highlight-color: transparent;
  }

  body {
    font-family: 'Cairo', sans-serif;
    background-color: #121212;
    color: #4a4a4a;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
  }

  .mobile-container {
    width: 100%;
    max-width: 430px;
    height: 100vh;
    max-height: 932px;
    background: #fdfbf7;
    position: relative;
    overflow: hidden;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
  }

  /* أبواب الغلاف والخلفية الخارجية */
  .door-left, .door-right {
    position: absolute;
    top: 0;
    width: 50%;
    height: 100%;
    /* صورة خلفية الغلاف الخارجي - يمكنك تغيير outer-bg.jpg */
    background: linear-gradient(135deg, #fefdfa 0%, #f7f2e8 100%), url('outer-bg.jpg');
    background-size: cover;
    background-position: center;
    z-index: 20;
    transition: transform 1.2s cubic-bezier(0.77, 0, 0.175, 1);
    box-shadow: 0 0 15px rgba(0,0,0,0.1);
  }

  .door-left { left: 0; border-left: 2px solid rgba(212, 175, 55, 0.4); }
  .door-right { right: 0; border-right: 2px solid rgba(212, 175, 55, 0.4); }

  .mobile-container.open .door-left { transform: translateX(-100%); }
  .mobile-container.open .door-right { transform: translateX(100%); }

  /* الدائرة الذهبية بالوسط */
  .interactive-seal {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 210px;
    height: 210px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.95);
    border: 2px solid #d4af37;
    box-shadow: 0 10px 30px rgba(212, 175, 55, 0.3);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    z-index: 25;
    transition: all 0.8s ease;
    text-align: center;
  }

  .mobile-container.open .interactive-seal {
    opacity: 0;
    visibility: hidden;
    transform: translate(-50%, -50%) scale(1.2);
  }

  .seal-names {
    font-family: 'Aref Ruqaa', serif;
    font-size: 2rem;
    color: #2c2c2c;
    line-height: 1.2;
    font-weight: 700;
  }

  .seal-names span { color: #c5a059; font-size: 1.3rem; display: block; }
  .seal-btn { font-size: 0.8rem; color: #8c733e; margin-top: 10px; background: rgba(212, 175, 55, 0.12); padding: 4px 12px; border-radius: 20px; }

  /* المحتوى الداخلي مع التمرير (Scroll) */
  .main-content {
    height: 100%;
    overflow-y: auto;
    scroll-snap-type: y mandatory;
    /* صورة الخلفية الداخلية - يمكنك تغيير inner-bg.jpg */
    background: linear-gradient(rgba(253, 251, 247, 0.85), rgba(253, 251, 247, 0.85)), url('inner-bg.jpg');
    background-size: cover;
    background-position: center;
  }

  .page-section {
    min-height: 100vh;
    scroll-snap-align: start;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 40px 24px;
    text-align: center;
  }

  .card-box {
    background: rgba(255, 255, 255, 0.92);
    backdrop-filter: blur(8px);
    border-radius: 24px;
    padding: 35px 22px;
    border: 1px solid rgba(212, 175, 55, 0.35);
    box-shadow: 0 10px 30px rgba(0,0,0,0.04);
    width: 100%;
  }

  .bismillah { font-family: 'Amiri', serif; font-size: 1.4rem; color: #8c733e; margin-bottom: 15px; font-weight: 700; }
  .invitation-text { font-size: 1.05rem; color: #555; line-height: 1.9; margin-bottom: 20px; }
  .couple-title { font-family: 'Aref Ruqaa', serif; font-size: 2.6rem; color: #222; margin: 15px 0; }
  .scroll-hint { font-size: 0.85rem; color: #a38139; margin-top: 25px; animation: bounce 2s infinite; }

  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
    40% { transform: translateY(-8px); }
    60% { transform: translateY(-4px); }
  }

  .date-badge {
    display: inline-block;
    background: #f7f2e8;
    border: 1px dashed #c5a059;
    padding: 10px 22px;
    border-radius: 50px;
    color: #6a5323;
    font-weight: 700;
    font-size: 1.1rem;
    margin-top: 15px;
  }

  .btn-action {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    background: linear-gradient(135deg, #d4af37 0%, #aa820a 100%);
    color: white;
    text-decoration: none;
    padding: 14px 28px;
    border-radius: 50px;
    font-weight: 700;
    font-size: 1.05rem;
    box-shadow: 0 8px 20px rgba(170, 130, 10, 0.25);
    border: none;
    cursor: pointer;
    width: 100%;
    margin-top: 20px;
  }

  .audio-control {
    position: absolute;
    top: 15px;
    left: 15px;
    z-index: 30;
    background: rgba(255,255,255,0.85);
    border: 1px solid #d4af37;
    border-radius: 50%;
    width: 38px;
    height: 38px;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
  }
</style>
</head>
<body>

  <div class="mobile-container" id="container">
    
    <div class="audio-control" id="audioBtn">🎵</div>

    <!-- الغلاف الخارجي -->
    <div class="door-left"></div>
    <div class="door-right"></div>

    <div class="interactive-seal" id="seal">
      <div class="seal-names">
        ياسمين
        <span>&amp;</span>
        أحمد
      </div>
      <div class="seal-btn">اضغط لفتح الدعوة ✨</div>
    </div>

    <!-- المحتوى الداخلي المقسّم لصفحات بالنزول -->
    <div class="main-content">
      
      <!-- الصفحة الأولى: الدعاء والترحيب -->
      <section class="page-section">
        <div class="card-box">
          <div class="bismillah">بسم الله الرحمن الرحيم</div>
          <p style="font-family:'Amiri', serif; font-size:1.2rem; color:#8c733e; margin-bottom:15px;">
            "وَمِنْ آيَاتِهِ أَنْ خَلَقَ لَكُم مِّنْ أَنفُسِكُمْ أَزْوَاجًا لِّتَسْكُنُوا إِلَيْهَا وَجَعَلَ بَيْنَكُم مَّوَدَّةً وَرَحْمَةً"
          </p>
          <p class="invitation-text">
            يسرنا ويسعدنا دعوتكم لحضور حفل زفافنا وتكتمل فرحتنا بوجودكم ومشاركتكم أجمَل لحظات العمر.
          </p>
          <div class="couple-title">ياسمين &amp; أحمد</div>
          <div class="scroll-hint">اسحبي للأسفل لرؤية التاريخ والموقع ↓</div>
        </div>
      </section>

      <!-- الصفحة الثانية: التاريخ -->
      <section class="page-section">
        <div class="card-box">
          <h2 style="font-family:'Aref Ruqaa', serif; font-size:2rem; color:#2c2c2c; margin-bottom:15px;">موعد الحفل</h2>
          <p class="invitation-text">ننتظركم بشوق في يومنا المميز</p>
          <div class="date-badge">
            🗓 السبت، 24 أكتوبر 2026م
          </div>
          <div class="scroll-hint">اسحبي للأسفل لموقع القاعة ↓</div>
        </div>
      </section>

      <!-- الصفحة الثالثة: الموقع -->
      <section class="page-section">
        <div class="card-box">
          <h2 style="font-family:'Aref Ruqaa', serif; font-size:2rem; color:#2c2c2c; margin-bottom:15px;">موقع الحفل</h2>
          <p style="color:#666; font-size:1.1rem; margin-bottom:10px;">قاعة الرتز - مدينة الرياض</p>
          <a href="https://maps.google.com" target="_blank" class="btn-action">
            📍 موقع القاعة على الخريطة
          </a>
        </div>
      </section>

    </div>
  </div>

  <audio id="bgMusic" loop>
    <source src="song.mp3" type="audio/mpeg">
  </audio>

  <script>
    const seal = document.getElementById('seal');
    const container = document.getElementById('container');
    const bgMusic = document.getElementById('bgMusic');
    const audioBtn = document.getElementById('audioBtn');
    let isPlaying = false;

    seal.addEventListener('click', () => {
      container.classList.add('open');
      bgMusic.play().then(() => {
        isPlaying = true;
      }).catch(e => console.log("Audio play blocked"));
    });

    audioBtn.addEventListener('click', () => {
      if (isPlaying) {
        bgMusic.pause();
        isPlaying = false;
        audioBtn.innerText = '🔇';
      } else {
        bgMusic.play();
        isPlaying = true;
        audioBtn.innerText = '🎵';
      }
    });
  </script>
</body>
</html>
